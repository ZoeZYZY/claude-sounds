# User-installed sounds (generated by install into ~/.claude)
# NOT the bundled themes in this repo
*.bak
*.bak.*

# User config (generated on install)
config.sh
config.ps1

# macOS
.DS_Store

# Windows
Thumbs.db
